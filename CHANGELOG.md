# 更新日志

## v1.38.1 (2026-08-31)

### 修复
- 修复 PHP 7.3 及更低版本下 `api.php` 因箭头函数（PHP 7.4+ 语法）整文件解析失败、接口返回 HTML，导致"加载失败: Unexpected token < in JSON at position 0"的问题（改为普通匿名函数，兼容 PHP 7.0+）。

## v1.38.0 (2026-08-28)

### 改进
- 预览模式也显示行号：切换预览时左侧行号栏保留，行号与源码逻辑行对应，样式与预览内容垂直对齐，并随预览内容同步滚动。

## v1.37.0 (2026-08-28)

### 新增
- Markdown 自动识别裸 URL：笔记中直接粘贴的网址（http/https/ftp、`www.` 开头）无需 `[]()` 包裹即可自动变为可点击链接，点击在新窗口打开。编辑器预览与分享只读视图均已支持；代码块/行内代码/图片地址内的内容不会被误识别。

## v1.36.0 (2026-08-24)

### 新增
- 服务器环境检测页 `check.php`：独立运行、不依赖主程序，部署报 500（尤其群晖 Web Station）时可直接访问该页定位原因。检测 PHP 版本/必需扩展/推荐扩展、AES-256-GCM 加解密实测、SQLite 读写实测、数据目录权限与位置、核心文件完整性、open_basedir 与错误日志配置，并汇总已知 500 触发点与群晖部署要点。排查完请删除该文件。

## v1.35.1 (2026-08-22)

### 修复
- 同一服务器/域名部署多套实例时登录互相顶掉：会话 Cookie 固定使用 `JSBSESSID`，多套程序同域共享同名 Cookie 导致互相覆盖。现改为按安装路径（`__DIR__`）自动生成唯一会话名，一台服务器任意多套部署互不冲突（部署后已登录会话需重新登录一次）

## v1.35.0 (2026-08-18)

### 新增
- **数据加密（AES-256-GCM）**：笔记标题、正文、TOTP 密钥在写入数据库前加密。密钥为 32 字节随机数，存于 Web 目录之外的密钥文件（`config.php` 中 `enc_key_path`，默认 `data_dir/key/master.key`）。即使数据库文件（含自动备份）泄露，没有密钥文件也无法读取任何内容
- **备份加密**：`doBackup()` 自动备份改为加密存储（`notes_*.db.enc`），加密后立即删除明文临时副本；后台备份列表兼容旧版明文备份
- **上传文件鉴权代理**：新增 `file.php`，图片/PDF 统一经 PHP 鉴权输出——登录用户仅能访问自己的文件，分享链接通过 `&share=令牌` 校验；不再存在无鉴权静态 URL
- **禁用 SVG 上传**：SVG 可内嵌脚本（存储型 XSS 风险），上传类型白名单移除 `image/svg+xml`
- **安全部署自检**：启动时检测数据目录与密钥文件是否位于 Web 根目录内，是则在管理后台醒目告警
- **三环境部署文档**：README 补充内网 WindowsServer+phpStudy、群晖 NAS+WebStation、外网 Debian12+宝塔的部署与 Nginx/Apache 防护配置示例

### 调整
- 搜索与「忘记密码关键词验证」改为**解密后在内存中匹配**（内容加密后 SQL `LIKE` 已无法使用）；列表按标题排序改为内存排序
- 上传目录、图片引用检测（`listUploadedImages`）随数据目录迁移到 `$config['data_dir']`，并在内容解密后匹配

### 修复
- Nginx 环境下 `data/notes.db` 与 `data/backups/*.db` 可被直接 URL 下载的隐患（数据目录外置 + 加密双重防护；详见 README 部署指引）

## v1.34.1 (2026-08-15)

### 修复
- 分享链接页面内容较短时底部残留大片空白：分享卡片改为撑满整个视口高度（flex 布局），文章短时卡片填满屏幕不再留白，文章长时内容区内部滚动；链接无效/过期提示页同步改为垂直居中

## v1.34.0 (2026-08-15)

### 新增
- 分享链接加固：仅支持分享单篇笔记，生成链接时必须选择具体笔记，分享页面只渲染该篇内容；旧版「全量分享」链接（会暴露分享者全部笔记）出于隐私安全已停用，需重新生成单篇分享链接
- 管理员重置密码后强制用户下次登录修改密码：用户登录后必须先校验当前密码并设置新密码才能进入记事本，未完成改密前页面操作全部拦截；用户在设置中自行修改密码同样视为完成强制要求

### 修复
- 无

## v1.33.4 (2026-08-15)

### 修复
- 个人开启 2FA 的账号登录未要求动态码验证：`doLogin()` 原仅在全局强制开关开启时才检查个人绑定状态，现改为账号已绑定 TOTP 则无论全局开关与否都必须验证动态码，未绑定且全局开启时才走绑定流程

## v1.33.3 (2026-08-13)

### 修复
- 石墨灰主题正文仍偏暗难以阅读：改为「浅灰画布 + 深灰文字」配色策略，正文背景提亮至 `#3d3d45`、文字加深至 `#1a1a20`，行号列、Markdown 预览区、状态栏、空状态同步适配，对比清晰且保持暗色外壳不刺眼

## v1.33.2 (2026-08-13)

### 修复
- 石墨灰主题正文文字对比度不足：编辑区正文、标题输入框、笔记列表标题由 `#c8c8d0` 提亮至 `#dcdce4`，暗色背景上清晰可读且不刺眼

## v1.33.1 (2026-08-13)

### 调整
- 「牛皮纸」皮肤改为「石墨灰」暗色主题：整体偏暗的中性灰配色，去除亮黄等刺眼颜色，降低长时间写作的视觉刺激

### 修复
- 无

## v1.33.0 (2026-08-12)

### 新增
- 分享链接改为账号自助管理：普通用户可在记事本页面的「分享」中自行生成只读分享链接（备注 + 有效期），并可查看、吊销自己的链接；管理员后台不再代建链接，仅保留全局查看与吊销

### 修复
- 无

## v1.32.1 (2026-08-12)

### 修复
- 修复数据库初始化遗漏 `share_tokens` 表的建表语句，导致后台管理页 500（表现为普通账号可登录、管理员登录后报错）

## v1.32.0 (2026-08-12)

### 新增
- 二次认证（2FA/TOTP）：兼容 Google / Microsoft / Authy 等标准 Authenticator；登录需输入 6 位动态码；未绑定用户首次登录引导绑定；生成 10 个一次性恢复码
- 后台「二次认证」开关：内网可关闭；切换「外网安全模式」自动开启、「内网便捷模式」自动关闭
- 连续输错 5 次动态码锁定 10 分钟；用户管理中可重置用户 2FA 绑定
- 只读分享链接：后台一键生成免登录只读链接（1小时/24小时/7天/30天可选），服务端强制只读、到期或吊销后自动失效

### 修复
- 无

## v1.31.2 (2026-08-03)

### 修复
- 验证码"换一个"改为 AJAX 方式刷新，不再清空已填写的用户名和密码

## v1.31.1 (2026-08-03)

### 修复
- 验证码标签 "验证码 —" 改为 "验证码："，避免破折号与算式混淆
- 修复点击 "换一个" 验证码不变的问题 — 每次 GET 加载时自动刷新验证码

## v1.31.0 (2026-08-03)

### 安全加固
- 新增 HTTP 安全响应头：CSP、X-Frame-Options、X-Content-Type-Options、Referrer-Policy、Permissions-Policy
- 新增登录数学验证码（CAPTCHA）— 后台安全设置可开关，内网模式自动关闭、外网模式自动开启
- 新增 `data/` 目录 Web 访问保护（.htaccess + web.config），防止数据库文件被直接下载
- 忘记密码关键词验证增加 IP 限速和 3 秒冷却间隔，防止暴力枚举
- 已验证：session_regenerate_id（防会话固定）、SameSite=Lax、CSRF token 均已就位

## v1.30.0 (2026-08-01)

### 修复
- 修复多行连续引用在阅读模式显示为多个独立引用框的问题 — 连续 `>` 行合并到同一个 `<blockquote>`
- 修复阅读模式切回编辑模式时页面跳到全文结尾的问题 — 记住并恢复切换前的滚动位置

## v1.29.6 (2026-08-01)

### 修复
- 修复多行选中时点击引用/列表按钮只有首行生效的问题 — 块级标记（引用 >、列表 -/1.）自动应用到每一行

## v1.29.5 (2026-08-01)

### 修复
- 修复编辑器中选中文本后点击 Markdown 工具栏按钮（行内代码、引用等）导致页面跳到全文结尾的问题 — 改用 `setRangeText` 替代全值赋值，避免滚动位置丢失

## v1.29.4 (2026-07-31)

### 修复
- 修复移动端皮肤/字体/字号/自动保存弹窗只显示遮罩不显示内容的问题（选择器临时移出 .editor-area 层叠上下文）

## v1.29.3 (2026-07-31)

### 修复
- 修复移动端「皮肤/字体/字号/自动保存」按钮无响应的问题 — 选择器改为居中弹窗+遮罩层，不依赖桌面端隐藏按钮定位

## v1.29.2 (2026-07-31)

### 优化
- 插入图片默认尺寸改为「中」，插入时始终写入尺寸标记（包括「大」），尺寸更直观可控
- 移动端点击 PDF 改用新标签页打开，利用浏览器原生 PDF 阅读器，避免 iframe 触发下载

## v1.29.1 (2026-07-31)

### 修复
- 修复灯箱双击 1:1 切换与单击关闭冲突（改用延迟检测区分单/双击）

## v1.29.0 (2026-07-31)

### 新增
- 灯箱图片交互增强 — 滚轮缩放、逐级放大/缩小按钮、1:1 原始尺寸、适应屏幕
- 缩放超过 100% 后支持拖拽平移，双击图片在 1:1 与适应之间切换
- 底部工具栏显示当前缩放百分比，毛玻璃半透明设计

## v1.28.5 (2026-07-31)

### 修复
- 修复灯箱缩放动画导致 PC 端无法打开的问题（移除冲突的内联 `display:none`）

## v1.28.4 (2026-07-31)

### 新增
- 灯箱图片展示添加缩放过渡动画（0.7→1 scale + 淡入淡出，cubic-bezier 缓动）

## v1.28.3 (2026-07-31)

### 修复
- 修复移动端 FAB 按钮点击后面板不弹出的问题（`!important` 优先级冲突）
- 修复 FAB 按钮点击时出现方形聚焦框的问题（补全 `outline: none` 和消除点击高亮）

## v1.28.2 (2026-07-31)

### 修复
- 修复 PC 端错误显示移动端浮动按钮和面板的问题（补全桌面端默认隐藏规则）

## v1.28.1 (2026-07-31)

### 优化
- **移动端预览/编辑切换优化**：顶部栏常驻「预览/编辑」切换按钮替代保存按钮，底部面板增加「保存」按钮，半透明观察模式方便快速审阅，切换预览自动保存

## v1.28.0 (2026-07-31)

### 新增
- **移动端操作面板**：工具栏按钮改为浮动 FAB「⋮」触发底部面板，分组展示编辑工具 / 笔记操作 / 工具外观
- **侧边栏↔编辑器滑动过渡**：从右侧滑入/滑出，侧边栏同步淡出+微缩放，视觉层次分明
- 底部面板支持全部暗色皮肤适配

## v1.27.0 (2026-07-29)

### 新增
- **移动端响应式适配**：手机/平板可正常使用，不会只显示左侧目录
  - 笔记页面：移动端侧边栏和编辑器分离为独立全屏视图，打开笔记自动切换到编辑模式
  - 编辑器头部新增「返回列表」按钮，点击退回笔记列表
  - 触控优化：所有按钮最小 38px，输入框放大防止 iOS 缩放，tooltip 自动隐藏
  - 登录页面：卡片全宽自适应、输入框/按钮放大、装饰元素缩小
- 补全 `skin-green` / `skin-warm` 搜索框、搜索结果信息、版本链接等搜索区域配色

## v1.26.1 (2026-07-29)

### 修复
- **护眼绿/暖黄纸搜索框背景色不跟随主题**：补全 `skin-green` 和 `skin-warm` 的搜索框、搜索图标、输入框、清除按钮、搜索结果信息、版本链接等全部配色

## v1.26.1 (2026-07-29)

### 修复
- **data_dir 不可写时给出明确错误提示**：当配置的 `data_dir` 目录不存在或无写入权限时，不再返回 500 白屏，而是输出具体的错误信息和修复指令

## v1.26.0 (2026-07-29)

### 新增
- **统一数据目录配置**：`config.php` 新增 `$config['data_dir']`，可指定数据库、备份、会话、日志的存储根目录
- **VPS 安全部署支持**：将 `data_dir` 指向网站目录外（如 `/www/data/notes`），数据库和备份不暴露于公网

### 变更
- `$config['db_path']` 留空时自动使用 `data_dir/notes.db`，无需手动指定
- 备份目录、会话目录、日志文件均跟随 `data_dir`
- 上传文件仍存放在 Web 可访问的 `data/uploads/` 下
- 兼容现有部署：不设置 `data_dir` 时行为与旧版完全相同

## v1.25.3 (2026-07-28)

### 优化
- 从编辑模式切换到预览模式时，若内容有变更则自动保存，避免遗忘丢失

## v1.25.2 (2026-07-28)

### 修复
- **修复低版本 PHP 500 错误**：`init.php` 中两处箭头函数 `fn()` 替换为传统闭包，向下兼容 PHP 7.0+

## v1.25.1 (2026-07-28)

### 修复
- **修复「选择字号」面板异常显示**：图片尺寸选择器 CSS 类名 `.size-selector` 与字体字号下拉面板冲突，导致下拉面板 `display:none` 被覆盖。重命名为 `.img-size-selector` 解决 ID/CSS 类名碰撞
- **附件插入图片补充尺寸选择**：从附件面板插入图片时，点击 ➕ 按钮先弹出 大/中/小 尺寸选择，再插入对应尺寸的 Markdown 语法

## v1.25.0 (2026-07-28)

### 新增
- **附件复用**：附件管理面板每条记录新增「插入」按钮，点击可将已上传文件直接插入到当前编辑的文章光标位置
- **图片尺寸选择**：插入图片时可选择大（全宽）/ 中（2/3宽）/ 小（1/3宽）三种尺寸，使用 `{s}` `{m}` 语法标记
- 编辑模式下的 Markdown 工具栏样式与各主题协调

## v1.24.0 (2026-07-28)

### 新增
- **支持 PDF 文件上传**：PDF 上传后在预览区渲染为图标块，点击使用灯箱 iframe 打开
- **前台新增「附件管理」面板**：每个用户可在侧边栏底部点击「附件」按钮，管理自己上传的所有文件
- 附件面板支持缩略图预览（图片）或 PDF 图标预览，显示引用状态和文件大小

### 优化
- 灯箱从纯图片升级为图片+PDF 双模式，PDF 通过内嵌 iframe 使用浏览器原生阅读
- 上传文件类型限制为图片（JPG/PNG/GIF/WebP/SVG/BMP）+ PDF，其他格式不支持
- 文件选择器 accept 更新，提示文字同步更新

### 安全
- 前台附件管理中用户只能查看和删除自己上传的文件，无权访问他人文件

## v1.23.0 (2026-07-28)

### 新增
- **管理面板新增「上传图片管理」模块**：可浏览所有已上传图片、查看引用状态、删除孤立文件
- 图片列表支持缩略图预览、按用户筛选、显示引用笔记编号
- 自动检测孤立图片（未被任何笔记引用的文件），橙色标记提示
- API 新增 `listImages`（图片列表）和 `deleteImage`（删除图片）接口

### 优化
- 图片弹窗「选择文件」按钮改为统一风格的样式化按钮，显示已选文件名
- 暗色皮肤适配文件选择按钮配色

## v1.22.4 (2026-07-28)

### 修复
- **Ctrl+D / 工具栏插入分隔符时移除前置空行**：分隔线 `---` 不再额外插入空行

## v1.22.3 (2026-07-28)

### 修复
- **修复预览区滚动条不跟随主题配色**：为 `.preview-content` 补全了全部 10 个皮肤变体的滚动条样式
- **修复 Markdown 工具栏配色与主题不一致**：为 6 个亮色皮肤（护眼绿/暖黄纸/牛皮纸/樱花粉/薰衣草/蜜桃）补充工具栏、按钮及悬停配色

## v1.22.2 (2026-07-28)

### 修复
- **修复图片上传 CSRF Token 校验失败**：上传函数使用了未定义的 JS 变量导致 token 无效
- 图片上传改用 FormData 字段方式传 csrf_token，与其他接口保持一致

### 新增
- 管理面板新增数据库压缩（VACUUM）功能及数据库大小显示
- SQLite 删除数据后会自动执行 VACUUM 回收磁盘空间

## v1.22.1 (2026-07-28)

### 修复
- **修复图片上传失败时 base64 数据被写入文章导致数据库膨胀**：上传失败后不再将 base64 DataURL 作为兜底插入笔记内容
- 移除管理员上传图片的限制

## v1.22.0 (2026-07-28)

### 新增
- **Markdown 编辑/预览模式**：笔记支持 Markdown 语法，默认预览模式展示渲染效果
  - 支持标题（H1-H3）、加粗、斜体、行内代码、引用块、分隔线、列表
  - 编辑区工具栏提供 Markdown 快捷插入按钮（B/I/H1/H2/H3/代码/引用/列表/分隔线）
  - 一键切换编辑/预览模式，新建笔记默认编辑模式，打开已有笔记默认预览模式
  - 快捷键支持：Ctrl+B 加粗、Ctrl+I 斜体、Ctrl+D 分隔符
- **支持在笔记中插入图片**：`![描述](URL)` Markdown 语法
  - 工具栏 📷 按钮弹出插图片弹窗，支持填写 URL 或选择本地文件
  - 预览区图片块级展示，悬停微缩放，点击弹出灯箱查看大图
  - 支持 JPG/PNG/GIF/WebP/SVG/BMP 格式上传（最大10MB）
  - 图片上传至 `data/uploads/{用户ID}/` 目录
- 分隔符改为 Markdown `---` 语法，预览时渲染为 `<hr>`

## v1.21.0 (2026-07-28)

### 新增
- **单客户端登录**：管理员后台可开启，新设备登录后旧设备自动被强制登出
- 被踢下线的用户登录页会显示「账号已在另一台设备登录」提示
- 机制：每次登录生成唯一 session_token，每次请求校验，不匹配立即登出

## v1.20.3 (2026-07-21)

### 修复
- 修复管理后台顶部显示 `$pageTitleSuffix = '管理后台'` 源码的问题（缺少 `<?php` 开标签）

## v1.20.2 (2026-07-20)

### 修复
- 修复「保持登录」用户切回浏览器标签页时被误判超时踢出（visibilitychange 未检查 KEEP_LOGIN 标志）
- 保持登录用户切换标签页时不再触发笔记列表重载，避免不必要的「类刷新」闪烁

## v1.20.1 (2026-07-20)

### 修复
- 修复「保持登录」勾选后仍被登出的问题：PHP 默认 `session.gc_maxlifetime` 仅 1440 秒（24分钟），
  导致服务端 GC 提前清理会话文件。现已同步为 7 天，并改用项目独立会话目录避免其他应用干扰

## v1.20.0 (2026-07-20)

### 新增
- 登录页新增「保持登录」选项，勾选后跳过不活动超时检测，仅手动登出或会话过期时退出
- 适合局域网独立电脑场景，用户无需担心闲置超时被踢出

## v1.19.2 (2026-07-20)

### 变更
- `_header.php` 重命名为 `header.php`，遵循标准命名规范
- 新增根目录 `.htaccess`，禁止直接访问 `header.php`、`config.php`、`init.php`
- `header.php` 内置直接访问防护（PHP 端 + Apache 端双重保护）

## v1.19.1 (2026-07-18)

### 修复
- 修复 JS 文件残留 PHP 短标签导致脚本崩溃、笔记不显示、皮肤切换失灵等问题
- 修复 notes.css 提取时损坏的 `.shortcut-hint kbd` 空规则块

## v1.19.0 (2026-07-18)

### 新增
- **代码模块化拆分**：CSS/JS 从 PHP 文件提取为独立静态文件
  - `assets/css/notes.css` — 笔记页全部样式（~1600 行）
  - `assets/js/notes.js` — 笔记页全部脚本（~1100 行）
  - `assets/css/admin.css` — 管理后台样式
  - `assets/js/admin.js` — 管理后台脚本
  - `assets/css/login.css` — 登录页样式
  - `assets/css/login-alt.css` — 备用登录页样式
  - `assets/css/changelog.css` — 更新日志页样式
- **公共 HTML 头部**：新增 `header.php` 统一管理 DOCTYPE、meta、favicon
- **消除重复代码**：`index-alt.php` 改为引用 `auth.php`，减少 ~300 行重复认证逻辑

### 优化
- 各 PHP 文件大幅精简（notes.php: 3068→~450 行，admin.php: 1695→~540 行）
- JS 中 PHP 变量改为 `data-*` 属性传递，彻底分离前后端

## v1.18.3 (2026-07-17)

### 优化
- SQLite 新增 `busy_timeout=60000`，遇到写入锁时自动等待 60 秒重试，避免 `database is locked` 错误

## v1.18.2 (2026-07-17)

### 调整
- 笔记页侧边栏头部布局改为底部对齐（flex-end），LOGO 向上突出更醒目
- 用户名左间距增加 6px，与 LOGO 视觉分离更明显

## v1.18.1 (2026-07-17)

### 调整
- 移除 Ctrl+N 新建笔记快捷键，避免与浏览器新建窗口快捷键冲突
- 修复清理毛玻璃皮肤后残留无效 CSS 导致的底纹异常

## v1.18.0 (2026-07-17)

### 调整
- **登录页风格统一**：毛玻璃设计升为默认 `index.php`，原版紫色渐变保留为 `index-alt.php` 备用
- 提取共享认证逻辑至 `auth.php`，文件名更简短清晰

## v1.17.0 (2026-07-16)

### 优化
- 侧边栏用户名右侧添加分隔线并增大间距，不再紧贴 Logo
- 用户名改为胶囊按钮样式，点击即可打开修改密码弹窗
- 移除侧边栏底部修改密码图标，退出登录按钮放大并添加「退出登录」文字标签

## v1.16.0 (2026-07-16)

### 新增
- 密码重置安全升级：移除前台「忘记密码？」入口，改为管理员生成时效性重置链接（支持5分钟/10分钟/30分钟/3小时/8小时有效期）
- 管理员后台重置密码改为系统随机生成密码，支持一键复制
- 新增 `reset_links` 表追踪重置链接使用状态（待使用/已使用/已过期）
- 管理员后台「重置密码链接」管理板块：分页展示、实时倒计时、一键复制链接、清理过期/全部记录

### 优化
- 管理员重置密码通知改为持久化确认：关闭后不再每次登录重复提示，直到下次管理员重置
- 用户自主修改密码后自动确认通知（不再重复提示）
- 管理员重置密码通知横幅居中显示
- 左侧目录树与正文区域滚动条适配各皮肤主题配色

### 数据库
- `users` 表新增 `last_reset_acknowledged_at` 字段
- 新增 `reset_links` 表（token / user_id / created_by / expires_at / used_at）

## v1.15.1 (2026-07-15)

### 修复
- 管理员重置密码通知横幅导致页面主体被挤偏的布局 BUG

### 新增
- 用户可在侧边栏底部「修改密码」自行修改密码，无需管理员介入

## v1.15.0 (2026-07-15)

### 新增
- 用户自助重置密码：忘记密码时通过笔记关键词验证身份后自行重置密码，无需管理员介入
- 管理员后台重置密码时记录审计日志，用户登录后可看到通知提示
- 新增 `password_reset_log` 表用于追踪密码重置记录

## v1.14.7 (2026-07-15)

### 修复
- 编辑器行号改为按回车换行符计数（非视觉折行），长行自动折行时插入空白行以保持对齐

## v1.14.6 (2026-07-15)

### 修复
- 修复 textarea 自动折行导致行号与内容视觉错位的BUG

## v1.14.5 (2026-07-15)

### 修复
- 浏览器最小化/后台时倒计时冻结、超时不登出的BUG，改为基于时间戳差值计算空闲时长

## v1.14.4 (2026-07-15)

### 优化
- 粉嫩系+牛皮纸主题的搜索框外背景改为浅色，与侧边栏统一

## v1.14.3 (2026-07-15)

### 修复
- 补全粉嫩系+牛皮纸四套主题的搜索框、退出按钮、功能图标、下拉菜单等 UI 组件配色，解决切换主题后组件颜色突兀的问题

## v1.14.2 (2026-07-15)

### 优化
- 牛皮纸主题改用真实噪点纸纹质感，替换原有线条底纹
- "深色模式"主题重命名为"暗夜黑"，统一三字命名

## v1.14.1 (2026-07-15)

### 移除
- 删除 暗海蓝、网格白底、网格绿底、暗网格 四套主题

## v1.14.0 (2026-07-15)

### 新增
- 三套粉嫩系配色主题：**樱花粉**（柔和粉调）、**薰衣草**（淡紫优雅）、**蜜桃橘**（暖橘甜美）

## v1.13.3 (2026-07-15)

### 修复
- 切换主题后行号栏颜色不同步，需刷新页面才生效

## v1.13.2 (2026-07-15)

### 修复
- 行号区域撑破编辑器布局，导致状态栏被挤出页面、滚动条失效

## v1.13.1 (2026-07-15)

### 优化
- 会话倒计时增加"空闲超时："前缀，含义更清晰

## v1.13.0 (2026-07-15)

### 新增
- **会话倒计时**：退出登录按钮上方显示实时倒计时，>1分钟显示"X分"，<1分钟显示"X秒"，操作自动刷新
- **编辑器行号**：左侧行号栏，与编辑器内容同步滚动，适配所有11种皮肤主题

## v1.12.0 (2026-07-14)

### 新增
- **登录日志分页**：每页显示条数可切换（20/50/100），支持翻页浏览
- **登录日志清理**：支持按日期清理（选择日期前全部删除），或按数量保留最新 N 条（100/200/300 或自定义）
- **数据库备份清理**：支持按天数清理旧备份（保留 7/15/30 天，或自定义天数）

## v1.11.5 (2026-07-12)

### 修复
- **彻底重写会话超时机制**：废弃不可靠的服务端心跳检测，改用客户端秒级空闲计时器
- 任何键鼠/触屏操作自动重置计时，到点立即跳转登录页（零延迟，无网络盲区）
- 服务端超时判定 `>` → `>=`，杜绝边界不触发问题
- API 请求超时返回 401 而非 302 重定向，确保前端正确检测

## v1.11.4 (2026-07-12)

### 修复
- 修复心跳每 60 秒续期导致 session 永远不超时的问题
- 心跳改为仅键鼠有操作时才续期，无操作则跳过让服务端计时器正常倒计时
- 新增「只检查不续期」通道（X-Norenew 头），心跳暂停后主动探测超时并自动跳转登录页
- 标签页隐藏时自动暂停心跳

## v1.11.3 (2026-07-12)

### 修复
- session 超时后立即跳转登录页（无延迟），由登录页展示「会话已过期」提示，杜绝内容泄漏窗口

## v1.11.2 (2026-07-10)

### 修复
- 修复 session 超时后页面不会自动跳转登录的问题：前端新增会话心跳检测（每分钟验证一次）
- 修复浏览器从 bfcache 恢复标签页时 session 已过期但页面仍可操作的问题
- 修复自动保存模式在 session 过期时静默失败导致数据丢失的问题
- 所有 API 调用统一拦截 401 状态码，自动弹出提示并跳转登录页

## v1.11.1 (2026-07-04)

### 修复
- 修复生成邀请码时提示信息显示原始 HTML 标签的问题
- 邀请码生成后新增专属展示区块 + 一键复制按钮

## v1.11.0 (2026-07-04)

### 新增
- 部署模式：后台新增「部署模式」设置面板，一键在内网便捷模式和外网安全模式之间切换
  - 内网便捷模式：开放注册、无登录限速、密码最短4位
  - 外网安全模式：仅邀请注册、登录限速（5次失败锁定15分钟）、密码最短8位
- 邀请码系统：外网安全模式下管理员可生成/删除邀请码，用户注册须提供有效邀请码
- 登录暴力破解防护：启用后同一 IP 在规定次数内连续失败将临时锁定
- 自定义安全设置：支持单独调整注册模式（开放/邀请/关闭）、密码长度限制、登录限速参数

### 修复
- 修复 api.php 中 updated_at 和 deleted_at 仍使用 SQLite CURRENT_TIMESTAMP（UTC）的时区 Bug，统一改用 PHP date() 传入本地时间

## v1.10.1 (2026-07-04)

### 优化
- 管理员「修改密码」移至顶栏右上角，点击弹出模态框修改，不再占用卡片区域
- 回收站设置与登录超时设置合并为一行双列布局，后台卡片更紧凑

## v1.10.0 (2026-07-04)

### 新增
- 不活动自动登出：超过设定时间无操作自动退出登录，防止忘记登出的安全隐患
- 管理后台新增「登录超时设置」卡片，可配置自动登出时间（0=关闭，默认30分钟）
- 超时检测机制：每次页面操作刷新计时，AJAX 请求超时返回 401 供前端处理

### 修复
- 插入分隔符不再附加多余空行，仅插入纯分隔符符号

## v1.9.0 (2026-07-04)

### 新增
- 数据库自动备份：每 24 小时自动在 `data/backups/` 目录生成备份，保留最近 30 个，无需手动操作
- 管理后台新增「数据库备份」卡片，可查看上次备份时间、备份数量、备份文件列表，并支持一键手动备份

## v1.8.1 (2026-07-04)

### 优化
- 工具栏按钮重新分组排序：内容编辑（字体/字号/分隔符）│ 笔记操作（保存/删除/置顶/导出）│ 工具外观（自动保存/回收站/皮肤），操作逻辑更清晰

## v1.8.0 (2026-07-04)

### 新增
- 新增「插入分隔符」按钮和 Ctrl+D 快捷键，插入 Unicode 制表符连续横线（─），在纯文本中实现最接近分割线的视觉效果

## v1.7.4 (2026-07-04)

### 文档
- 重写 README.md，新增手动上传 GitHub 教程、升级指南、皮肤预览表
- 补充暗夜绿/暗海蓝/暖夜色/暗网格 4 套深色皮肤文档描述

## v1.7.3 (2026-07-04)

### 修复
- 修复选择暗夜绿/暗海蓝/暖夜色/暗网格 4 套深色皮肤时后端返回"无效的皮肤选项"的问题（白名单遗漏）

## v1.7.2 (2026-07-04)

### 改进
- 修复工具栏 tooltip 被 overflow:hidden 裁剪不可见的问题（改为向下弹出 + 小三角箭头）
- 补全 3 个遗漏按钮的 tooltip（自动保存、删除笔记、回收站），现全部 9 个按钮均使用自定义 tooltip

## v1.7.1 (2026-07-04)

### 改进
- 修复皮肤/字体等下拉菜单在最右侧按钮弹出时被容器遮挡的问题（自动检测溢出并右对齐）
- 工具栏按钮悬停提示改为 CSS 自定义 tooltip，鼠标移入即时显示，无延迟感

## v1.7.0 (2026-07-04)

### 新增功能
- 4套深色护眼皮肤：暗夜绿（终端风格）、暗海蓝（VS Code 风格）、暖夜色（夜间阅读）、暗网格（淡紫网格纹理）

### 改进
- 编辑器工具栏按钮改为纯图标 + 悬停提示（title），界面更简洁
- 底部状态栏（字数统计 + 快捷键提示）改为右对齐布局
- 移除侧边栏排序下拉框，简化侧边栏

## v1.6.0 (2026-07-04)

### 新增功能
- 笔记置顶：支持将重要笔记置顶显示，置顶笔记在列表中优先排列
- 笔记排序：支持按更新时间、创建时间、标题排序切换，偏好自动记忆
- 单篇 TXT 导出：可将当前笔记导出为 UTF-8 编码的 .txt 文件
- 编辑器字数统计：实时显示总字符数和不计空格字符数
- 键盘快捷键增强：新增 Ctrl+N 新建笔记、Ctrl+F 聚焦搜索框

### 改进
- 侧边栏新增排序下拉框，排序偏好自动存储到 localStorage
- 编辑器底部新增快捷键提示栏，降低学习成本
- 笔记列表置顶项显示 📌 标记，一目了然

## v1.5.1 (2026-07-04)

### 样式优化
- 删除按钮改为醒目的红色（常驻红色而非仅悬停），危险操作更直观
- 编辑器工具栏按钮重新排序：字体 → 字号 → 自动保存 → 保存 → 删除 → | → 回收站 → 皮肤
- 删除按钮与回收站之间增加竖线分隔符，区分常用操作与辅助功能

## v1.5.0 (2026-07-04)

### 新增功能
- 定时自动保存：用户可设置 1/2/3/5/10 分钟间隔或关闭，默认每 3 分钟自动保存一次
- 自动保存仅保存有变更的笔记（脏状态追踪），避免无意义请求
- 自动保存时可自动创建新笔记（未手动保存过的新笔记也会被保存）
- 自动保存 toast 提示为精简样式，与手动保存区分
- 新建笔记和切换笔记时自动重置脏标记

### 改进
- 编辑器头部新增「自动保存」按钮，与字体/字号/皮肤并列
- 自动保存选择器支持所有 7 套皮肤（含暗色模式）适配

## v1.4.0 (2026-07-04)

### 新增功能
- 登录访问统计：记录每次登录的用户名、IP、时间、成功/失败状态
- 管理后台新增登录访问日志面板，展示最近50条登录记录
- 管理后台新增登录成功/失败统计卡片
- 代理/负载均衡 IP 识别（支持 X-Forwarded-For）

### 布局优化
- 管理后台「修改管理员密码」和「回收站设置」改为双列并排布局
- 统计卡片精简尺寸，一排放5个，信息密度更高
- 表单改为更紧凑的风格，减少垂直空间占用
- 主容器宽度增至 1100px，适配更多内容

## v1.3.0 (2026-07-03)

### 改进
- 搜索框改为侧边栏常驻显示，无需点击按钮即可随时搜索
- 搜索输入支持 300ms 防抖，减少频繁请求
- 搜索框新增清除按钮（×）和 Escape 键清空搜索
- 搜索框 UI 优化：添加搜索图标、更美观的输入框样式
- 所有皮肤（含暗色模式）适配新搜索框样式

## v1.2.0 (2026-07-03)

### 新增功能
- 笔记回收站：删除笔记改为软删除，先移入回收站而非直接删除
- 回收站面板：可查看已删除笔记，显示删除时间和剩余保留天数
- 回收站操作：支持逐条恢复、逐条彻底删除、一键清空回收站
- 管理员回收站设置：可配置自动清空天数（默认30天，1-365可调）
- 过期回收站自动清理：系统以约 1/30 概率在请求时自动巡检清理
- 登录后默认自动打开最后编辑的那条笔记
- 数据库备份脚本 `backup.php`：支持命令行定时备份、自动清理旧备份

### 改进
- API 笔记列表/搜索/详情均已过滤回收站记录
- 管理后台新增回收站统计卡片

## v1.1.0 (2026-07-02)

### 新增功能
- 新增7套护眼皮肤（默认白、护眼绿、暖黄纸、深色模式、牛皮纸、网格白底、网格绿底）
- 新增字体设置功能（默认、宋体、楷体、仿宋、Consolas、Monaco）
- 新增字号调整功能（12px-24px）
- 新增笔记自定义标题功能，侧边栏显示标题名

### 样式优化
- 布局优化：居中显示、左右留白、添加阴影效果
- 设置按钮移至右上角保存按钮前面
- 修复侧边栏底部双横线问题

### 安全加固
- 会话Cookie添加HttpOnly、Secure、SameSite属性
- API接口添加CSRF防护

### 功能改进
- 搜索功能支持标题搜索

## v1.0.1 (2026-07-02)

### 样式优化
- 管理后台容器宽度加宽至 1000px，布局更舒展
- 操作列宽度增加，按钮文字不再折行（`white-space: nowrap`）
- 统计卡片字号加大，数字 28px → 36px，间距优化
- 表格单元格内边距增大，行高更舒适
- 卡片增加边框与阴影层次感

## v1.0.0 (2026-07-02)

### 首次发布
- 用户注册与登录系统，支持多用户数据隔离
- 记事本核心功能：新建、编辑、保存、删除笔记
- 笔记列表分页展示，按更新时间排序
- 笔记内容搜索功能
- 管理员后台：查看用户列表、重置用户密码
- 管理员账号不参与记事，仅负责用户管理
- 全部图标使用 SVG 绘制，无外部依赖
- SQLite 数据库存储，轻量部署
- PKCE 安全：CSRF Token、密码哈希、会话管理
- 操作日志记录
- 美观的渐变 UI 设计，响应式适配
